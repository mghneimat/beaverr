import { R } from '../../../constants/onboarding-theme';

/** Icon box + stroke — matches ad-hoc add (+) buttons */
export const CYCLE_CONTROL_ICON_BOX = 22;
export const CYCLE_CONTROL_ICON_STROKE = 2.5;

/** Full-width pill CTA — shared by all cycle control tiles */
export const CYCLE_CONTROL_PILL_BUTTON = {
  alignSelf: 'stretch',
  width: '100%',
  alignItems: 'center',
  justifyContent: 'center',
  paddingVertical: 10,
  paddingHorizontal: 16,
  minHeight: 44,
  borderRadius: R.button,
};
