/**
 * UI Components - GlueStack UI Wrappers
 * 
 * These wrappers provide a simplified API for common components
 * while maintaining compatibility with your existing code.
 * 
 * Import like: import { PrimaryButton, FormInput } from '@/components/ui'
 */

// Re-export all GlueStack UI components for direct use
export * from '@gluestack-ui/themed';

// Custom wrappers
export { PrimaryButton } from './PrimaryButton';
export { FormInput } from './FormInput';
export { OptionCard } from './OptionCard';
